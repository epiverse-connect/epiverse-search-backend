# Check if `<data.frame>` input is from `epireview`

```r
.is_epireview(x)
```

## Arguments

- `x`: A `<data.frame>`.

## Returns

A single `logical` boolean.

Check if `<data.frame>` input is from `epireview`
